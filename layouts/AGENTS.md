# LAYOUTS KNOWLEDGE BASE

## OVERVIEW

This directory currently exists but is empty.

## CONVENTIONS

- Keep it empty unless the project intentionally introduces a separate layouts layer.
- Prefer the existing `components/Layout.tsx` pattern for shared page chrome.

## ANTI-PATTERNS

- Do NOT add duplicate layout infrastructure here while `_app.tsx` + `components/Layout.tsx` already handle the site shell.
